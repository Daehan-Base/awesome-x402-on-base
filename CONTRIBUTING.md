# 기여 가이드

## 환영합니다! 👋

**Awesome x402 on Base** 프로젝트에 관심을 가져주셔서 감사합니다!

이 프로젝트는 Base 체인에서 x402 프로토콜을 사용하기 위한 한국어 생태계를 구축하고 있습니다. 문서 작성, 예제 코드 추가, 버그 리포트 등 어떤 형태의 기여도 환영합니다.

---

## 기여 유형

- 📝 **문서 기여** - 한글 튜토리얼, 가이드 작성
- 💻 **예제 코드** - Base 특화 예제 추가
- 🐛 **버그 리포트** - 이슈 등록
- 💡 **기능/예제 제안** - 아이디어 공유

---

## 시작하기

### 레포지토리 설정

```bash
# 서브모듈과 함께 클론
git clone --recursive https://github.com/Daehan-Base/awesome-x402-on-base.git
cd awesome-x402-on-base

# 또는 이미 클론한 경우 서브모듈 초기화
git submodule update --init --recursive
```

### 개발 환경

**Python:**
- Python 3.10+
- 패키지 매니저: `uv` (권장)

**TypeScript:**
- Node.js 18+
- 패키지 매니저: `pnpm` (필수)

---

## 기여 워크플로우

### 1. Issue 확인/생성

작업 시작 전에 관련 Issue가 있는지 확인하세요. 새로운 기능이나 큰 변경사항은 먼저 Issue를 생성하여 논의해주세요.

### 2. Fork & Clone

```bash
# Fork 후 클론
git clone --recursive https://github.com/YOUR_USERNAME/awesome-x402-on-base.git
```

### 3. Branch 생성

```bash
# 브랜치 명명 규칙
git checkout -b feat/기능명      # 새 기능
git checkout -b docs/문서명      # 문서 추가/수정
git checkout -b fix/버그명       # 버그 수정
```

### 4. 작업 수행

- 기존 코드 스타일을 따라주세요
- 테스트가 있다면 실행해주세요
- 문서를 함께 업데이트해주세요

### 5. PR 제출

```bash
git add .
git commit -m "feat: 기능 설명"
git push origin feat/기능명
```

GitHub에서 Pull Request를 생성하세요.

---

## 문서 기여 가이드라인

### v1/v2 구조

| 버전 | 위치 | 설명 |
|------|------|------|
| v1 (Legacy) | `docs/v1/` | 기존 SDK 문서 |
| v2 (Current) | `docs/v2/` | 새 프로토콜 문서 |

새 문서 작성 시 적절한 버전 디렉토리에 배치해주세요.

### 한글 문서 작성 규칙

1. **Web2 비유 사용** - 기술 개념 설명 시 익숙한 Web2 서비스와 비교
2. **작성 시점 명시** - 문서 상단에 작성일/검증일 포함
3. **보안 경고 포함** - Private Key, 테스트넷 사용 등 보안 관련 경고 필수

### 예제 문서 구조

```markdown
# 예제 제목

## 개요
예제 목적과 학습 목표

## 사전 요구사항
- 필요한 도구
- 환경 설정

## 단계별 가이드
1. 첫 번째 단계
2. 두 번째 단계
...

## 문제 해결
자주 발생하는 오류와 해결 방법
```

---

## 코드 기여 가이드라인

### Python

- **버전**: Python 3.10+
- **패키지 매니저**: `uv`
- **타입 힌트**: 사용 권장
- **비동기**: FastAPI/httpx는 `async/await` 사용

```python
# 예시
async def get_payment(client: httpx.AsyncClient) -> dict:
    response = await client.get("/payment")
    return response.json()
```

### TypeScript

- **Node.js**: 18.0.0+
- **패키지 매니저**: `pnpm` (필수)
- **모듈**: ESM (ECMAScript Modules)
- **strict mode**: 활성화

```typescript
// 예시
export async function getPayment(): Promise<Payment> {
  const response = await fetch("/payment");
  return response.json();
}
```

---

## ⚠️ 보안 규칙 (필독)

**절대 하지 말아야 할 것:**

- ❌ Private Key를 커밋하거나 코드에 하드코딩
- ❌ `.env` 파일 커밋
- ❌ `external/x402/` 디렉토리 수정 (읽기 전용 서브모듈)
- ❌ Mainnet 키를 개발에 사용

**반드시 지켜야 할 것:**

- ✅ Base Sepolia 테스트넷만 사용
- ✅ `.env` 파일에 시크릿 저장 (gitignored)
- ✅ 개발용/운영용 지갑 분리

자세한 보안 가이드라인은 [CLAUDE.md](./CLAUDE.md)를 참고하세요.

---

## 커밋 메시지 규칙

[Conventional Commits](https://www.conventionalcommits.org/) 형식을 따릅니다:

```
타입: 간단한 설명

본문 (선택사항)
```

**타입:**
- `feat`: 새 기능
- `docs`: 문서 추가/수정
- `fix`: 버그 수정
- `refactor`: 리팩토링
- `chore`: 기타 작업

**예시:**
```
feat: TypeScript 클라이언트 예제 추가

- x402 v2 SDK 사용
- Base Sepolia 테스트넷 연동
```

---

## 커밋 서명 (권장)

GPG 서명은 필수는 아니지만 권장합니다:

```bash
# GPG 서명 설정
git config --global commit.gpgsign true
```

설정 방법: [GitHub 커밋 서명 가이드](https://docs.github.com/en/authentication/managing-commit-signature-verification/signing-commits)

---

## PR 체크리스트

PR 제출 전 확인사항:

- [ ] 프로젝트 코드 스타일 준수
- [ ] 관련 문서 업데이트
- [ ] 보안 규칙 확인 (Private Key, .env 등)
- [ ] 새 기능의 경우 예제/테스트 포함

---

## 이슈 등록하기

새로운 기능이나 예제를 제안하려면 [기능/예제 제안 템플릿](https://github.com/Daehan-Base/awesome-x402-on-base/issues/new?template=feature_request.yml)을 사용해주세요.

**이슈 등록 시:**
- 제안 유형 선택 (Base 특화 예제, 한글 문서, 새 언어/프레임워크 등)
- 제안 내용과 필요한 이유 작성
- 직접 PR을 제출할 의향이 있다면 체크박스 선택

> 💡 **Tip**: 리뷰가 필요하면 이슈나 PR에서 maintainer를 태그해주세요!

---

## 도움 받기

- **질문/이슈**: [GitHub Issues](https://github.com/Daehan-Base/awesome-x402-on-base/issues)
- **토론/아이디어**: [GitHub Discussions](https://github.com/Daehan-Base/awesome-x402-on-base/discussions)

---

감사합니다! 🙏
